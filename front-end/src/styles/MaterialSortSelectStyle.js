const style = theme => ({
    root: {
        "& .right, .left": {
            display: "flex",
            margin: "10px 0"
        },
        "& .right": {
            justifyContent: "flex-end"
        }
    }
});

export default style;
